# #834 — audit of russell-ch4-8.049gGf2's V3 vs #820's 35.22 km/s truth residual: CONFIRMED NO CONTRADICTION, V3 stands; plus one new finding (#842/#847)

**Date:** 2026-08-12
**Task:** `#834` (registered by `#826`, dispatched 2026-08-12) — audit
`russell-ch4-8.049gGf2`'s **V3** `_LEVEL_EVIDENCE` entry
(`src/cyclerfinder/data/validate.py`, key `("russell-ch4-8.049gGf2", "V3")`) against `#820`'s
35.22 km/s truth residual for the same row under the corrected designated-arc genome posing
(`scripts/campaign_russell12.py`,
`data/runs/russell12-circular-20260811T-820-reposed.jsonl`). The registration's hypothesis —
"almost certainly not a contradiction" — was asserted, not verified; a V3 row is a
trust-bearing claim, so both halves were verified independently here rather than trusted.

**Verdict: (a) confirmed — no contradiction. The V3 stands. No `validation_level` value was
touched** (scope boundary honoured); a comment-only `ADDED EVIDENCE (…, no level change)`
annotation was added to the row per the `#826`/`#828` precedent. One NEW finding surfaced by
this audit's own re-run of the V3 evidence gate (a drifted regression anchor on an @slow
test) is registered as `#842`/`#847`, deliberately NOT resolved here.

## 1. What the V3 actually rests on (traced, not assumed)

The row's V3 is the `#175` **V3-POWERED** class-split verdict on Russell 2004's **App-C
parent #188** — a lane completely disjoint from `#820`'s probe:

| axis | V3 evidence lane (#170/#175) | #820's probe lane |
|---|---|---|
| model | real DE440 ephemeris | circular-coplanar idealisation |
| method | propagate App-C's PRINTED per-leg states (v∞ vectors + epochs + mid-leg Δv) on an independent REBOUND/IAS15 integrator; nothing fit | Lambert boundary-value corrector over the Table-4.9 descriptor genome |
| data | Russell 2004 Appendix C "DATA NECESSARY TO REPRODUCE" block #188 (line 8643) + Table 5.5 total Δv 420 m/s | Table 4.9 row 2 descriptors g(1.4951,538.24,Ll) / G(1.7757,279.24,U) / f(1:1,74.468,−180.000) |
| code | `src/cyclerfinder/search/appc_corrected.py` (verbatim transcription + `build_seeded_arcs` + `continuous_chain`) | `scripts/campaign_russell12.py::build_genome/probe_at_truth` |
| gate | `tests/nbody/test_appc_batch_nbody.py` (@slow) | `data/runs/russell12-circular-20260811T-820-reposed.jsonl` runlog |

Evidence chain: `#170` (`docs/notes/2026-06-08-appc-v3-batch-results.md`) reconstructed all
7 Mars encounters in-band (miss 3.2e-6…1.1e-5 AU ≤ 1,691 km ≪ 1 Mars-SOI) at the published
per-leg breathing v∞ (8.42…11.71) to ≤ 1e-3 km/s, verdict PARTIAL (over the 120 m/s
ballistic budget); `#175` (`docs/notes/2026-06-08-v3-powered-classsplit.md`) added the
V3-POWERED tier (continuous TCM ≤ documented Δv × 1.10) under which #188 PASSES
(163.6 ≤ 420×1.10 m/s at promotion time; re-derived to 114.4 m/s by the `#198` 63 s UTC/TDB
epoch fix, `docs/notes/2026-06-23-appc-s1l1-tcm-epoch-rederivation.md` — the correction is
recorded in the batch note's header but the `_LEVEL_EVIDENCE` text still cites 163.6, folded
into `#842`'s scope).

Nothing in this chain touches a Lambert problem, the circular model, or the Table-4.9
descriptor genome. Hypothesis half (a) is verified structurally.

## 2. The App-C evidence was RE-RUN, not trusted — and it surfaced a new finding

`uv run pytest "tests/nbody/test_appc_batch_nbody.py::test_appc_batch_encounters_in_band_but_published_dv_over_v3_budget[russell-ch4-8.049gGf2]" -m slow -q -n0`
(2026-08-12, this machine):

- **The evidence-bearing asserts still PASS**: all 7 Mars encounters converge, land in-band
  (< 0.0116 AU) on the independent Sun-only IAS15 at the published per-leg v∞ to abs=5e-3
  km/s, and the sourced published total Δv (420 m/s) still exceeds the 120 m/s ballistic
  budget (the binding V3-ballistic rejection / the reason it is V3-POWERED). The encounter
  geometry that carries the V3 is live and reproducible today.
- **The final regression-anchor assert FAILS**: the Sun-only proxy TCM measures **0.219 m/s
  against the pinned 114.4 m/s** (`abs=1.0`). The sibling parametrization
  (`russell-ch4-8.165Gfh-f2`, App-C #192) fails the same assert: **1263.5 vs 2020.7 m/s**.
  Per-node decomposition (this audit): #188's 27 node dvs are smooth ~0.005-0.03 m/s
  corrections with physical-looking misses; #192's 35 nodes are near-zero except four large
  burns (246/434/50/531 m/s) at large-miss nodes — structured like a real trajectory, not
  like a broken calculation. This test is @slow (excluded from the habitual `-m 'not slow'`
  ratchet), so the drift has no observed "last green" date; candidate causes in the window
  since the 2026-06-23 pinning: `77133d8d` (#692 `_AstropyBackend` batch/scalar
  BLAS-dispatch fix, 2026-07-23 — directly in this lane's ephemeris backend), `b1a0015e`
  (#824 SPICE kernel-pool stale-cache fix, 2026-08-10), or the Linux→macOS migration
  (documented cross-platform BLAS/DOP853 divergence class).

**Impact on THIS row's V3: none.** The V3-powered budget criterion is TCM ≤ 420×1.10 =
462 m/s; it holds under every historical value (163.6, 114.4, 0.219) — the drift direction
only strengthens it, and the binding encounter evidence passes unchanged. But an
evidence-bearing gate red on its own anchor is a real un-triaged finding, and the test's own
text commands "re-derive, do not silently re-pin" — registered as **`#842`** (root-cause +
re-derivation; NOT resolved here per the audit's scope) and **`#847`** (#192 flip-risk: its
V3-powered rejection was grounded on TCM 2040.6/2020.7 > 1678×1.10 = 1845.8 m/s, and the
current measure 1263.5 is UNDER that bar — if `#842` finds the current code correct, #192's
V0/absent floor needs honest re-adjudication).

## 3. #820's 35.22 km/s residual: independently reproduced and decomposed — it IS the #825 degeneracy

Reproduction (this audit, same functions `#820`/`#826` used: `build_genome` →
`select_leg1_topology` at 256 phase epochs, circular model): truth residual **35.2248**,
byte-identical to `#820`'s runlog record (`leg1_selection.truth_residual_kms: 35.2248`,
single leg-1 candidate `(0, single)`).

Per-node decomposition at the truth seed (`_vinf_nodes` at the selection's best t0, truth
ToFs [93.0, 555.6, 365.3], slack leg reconstructed to 545.7 d vs the sourced 546.1 — the
known ~1.3 d `period.years` print-rounding slack, sequence E-M-E-E-E):

| node | body | \|v∞ in\| | \|v∞ out\| | mismatch (km/s) |
|---|---|---|---|---|
| 0 | E | — | 27.593 | — |
| 1 | M | 30.220 | 16.672 | 13.548 |
| 2 | E | 20.055 | 43.187 | 23.131 |
| **3** | **E** | **43.187** | **7.962** | **35.225 ← the max residual** |
| 4 | E | 7.962 | — | — |

Per-leg conics recovered from the Lambert solutions at truth:

| leg | arc | ToF (d) | Lambert conic | sourced conic | endpoint transfer angle |
|---|---|---|---|---|---|
| 2 (E→E) | **f(1:1) loop** | 365.3, 1 rev | **a=1.1150 AU, e=1.0000** (pathological) | a=1.0000, e=0.2678 | **0.04° + 360° — degenerate** |
| 3 (E→E) | g(1.4951) loop | 545.7, 1 rev | a=1.0827 AU, e=0.2680 | a=1.0837, e=0.2708 | 177.9° + 360° — well-posed |

The reading is unambiguous:

- The max residual (35.225 — the reported 35.22) sits at **node 3, the f(1:1) loop leg's
  exit**. The 1:1 resonant E-E loop departs and re-meets Earth at the same point after one
  Earth year, so its Lambert problem has a ~360° transfer angle and is **degenerate**
  (endpoints+ToF cannot recover the printed loop e — exactly `#825`'s statement, and exactly
  what the row's own `loop-ee-1` segment note has recorded since `#794`: "the E-E
  boundary-value problem is degenerate at 360 deg"). The solver returns a pathological
  e=1.0000 conic instead of the sourced e=0.2678 loop, poisoning the adjoining nodes.
- In the circular model this degeneracy is **phase-independent** (an E→E leg of 365.3 d has
  ~360° geometry at every t0), so the "residual at truth" for this row can never see the
  row's actual geometry — it measures the genome's representability limit, nothing else.
- Where the genome IS well-posed the sourced geometry checks out: the g(1.4951) loop leg's
  Lambert solution independently reproduces the `#794`-derived conic to (Δa 0.001 AU,
  Δe 0.003) and emerges **v∞E = 7.962 vs the row's printed 8.05 anchor** at node 4.

Hypothesis half (b) is verified: `#820`'s 35.22 is the `#825` f(1:1) Lambert-degeneracy
wall — a limitation of `probe_at_truth`'s current genome, **not** a geometry claim about the
row — and it therefore cannot contradict a V3 that lives on the disjoint App-C real-eph lane
of §1. `#825` (resonant-loop-aware leg pinned from the segment's (a,e)) remains the fix path.

## 4. Deliverables

- **`data/catalogue.yaml`** — comment-only `ADDED EVIDENCE (#834 …, NO LEVEL CHANGE — V3
  stands)` annotation appended to the row's `validation_level: V3` comment
  (`#826`/`#828`/`#181` precedent). **The level value itself is untouched.**
- **`data/OUTSTANDING.md`** — `#834` → ✓ DONE; registered **`#842`** (root-cause + re-derive
  the drifted App-C proxy-TCM anchors; includes folding the post-`#198` corrected figure into
  the stale 163.6 in `_LEVEL_EVIDENCE`) and **`#847`** (#192 V3-powered flip-risk, gated on
  `#842`).
- This note.
- No code, no tests, no `validation_level` edits. The failing @slow asserts are left RED
  deliberately — they are the honest state and the forcing function for `#842`; re-pinning
  without a root cause is exactly what the test's own text forbids.

## 5. Verification

- `uv run ruff check .` / `uv run ruff format --check .` — clean.
- `uv run mypy src tests` — clean.
- `uv run pytest tests/data tests/search -q` — full ratchet (catalogue comment touched), see
  the `#834` bullet for the run record.
