# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.eggs/

# Build artifacts
build/
dist/
wheels/

# Virtualenvs
.venv/
venv/
env/

# Tool caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.cache/
.tox/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Project outputs (per spec.md §7 — runs land here)
out/

# Intermediate scan progress streams; the consolidated *.jsonl is the deliverable
*.stream.jsonl

# Editor / OS detritus
.idea/
.vscode/
.DS_Store

# Pre-existing stray file in the repo root; do not commit.
nohup.out

# Local Claude Code config / scheduler state
.claude/

# Playwright-MCP browser-automation session detritus (regenerated per run)
.playwright-mcp/

# Transient run-logs from detached long runs (scratch, not deliverables)
data/runlogs/
