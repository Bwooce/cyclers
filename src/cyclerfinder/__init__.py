"""cyclerfinder — systematic search and verification of planetary cycler trajectories.

See ``docs/spec.md`` for the canonical project specification and
``docs/overview.md`` for the milestone roadmap. Only the M0 scaffold and the
physical constants module are populated at this stage; the rest of the
architecture (ephemeris, lambert, kepler, flyby, search, model, verify, viz,
cli) lands in M1+ as each module is written.
"""

__version__ = "0.0.1"
